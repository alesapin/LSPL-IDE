#ifndef MAINWINDOW_H
#define MAINWINDOW_H
#include <centralwidget.h>
class MainWindow : public QMainWindow
{
    Q_OBJECT
private:
public:
    MainWindow(QWidget *parent = 0);
    ~MainWindow();
};

#endif // MAINWINDOW_H
